# Problem01: 構成と命名の妥当性レビュー

**作成日**: 2026-01-09
**対象**: React Template - Loading ページ
**目的**: 初心者エンジニアの基本的な見本として、命名と構成の妥当性を検証・改善

---

## 1. 発見された問題

### 1.1 ファイル・ディレクトリ構成の問題

#### 問題: Loading.jsx という命名
- **現状**: `src/pages/Loading.jsx` (ページコンポーネント扱い)
- **問題点**:
  - 「Loading」は機能名であり、ページ名ではない
  - 通常、`pages/` ディレクトリには「ページ単位」のコンポーネントを配置する（例: Home, About, Dashboard など）
  - Loading は複数のページで再利用される UI パターン
  - 現在は単一ページだけを表示する構成であるにもかかわらず、ページディレクトリに配置されている矛盾

#### 推奨: 構成の見直し
```
現在の構成:
src/
  pages/
    Loading.jsx          ← 単一ページでも「ページ」として扱われている
  styles/
    loading.css

推奨される構成:
src/
  components/           ← UI パターン・機能単位のコンポーネント
    loading/
      Loading.jsx
      loading.module.css  または loading.css
  pages/
    Home.jsx            ← 実際のページ
  styles/
    global.css          ← グローバルスタイル
```

**理由**:
- React の慣例では `components/` は再利用可能な小さな単位
- `pages/` はルーティング対象の実際のページ
- 今後アプリが成長し、複数ページがある場合の拡張性が向上
- 初心者が学ぶべき正しい構造

---

### 1.2 CSS ファイル構成の問題

#### 問題: CSS ファイルの配置と スコープ
- **現状**: `src/styles/loading.css` (グローバルスタイルディレクトリ)
- **問題点**:
  - スタイルがコンポーネントと分離されている
  - `styles/` ディレクトリには通常、グローバルスタイルや共有スタイルを配置
  - Loading コンポーネント専用のスタイルがグローバルディレクトリに存在する矛盾
  - 大規模化時にスタイルとコンポーネントの対応関係が不明確になる

#### 推奨:
```
構成1: CSS Modules パターン（推奨 - 最もスケーラブル）
src/
  components/
    loading/
      Loading.jsx
      Loading.module.css

構成2: CSS-in-JS パターン（styled-components など使用時）
src/
  components/
    loading/
      Loading.jsx
      styles.js

構成3: コンポーネント同じディレクトリ + スコープ付き CSS
src/
  components/
    loading/
      Loading.jsx
      loading.css  （クラス名をスコープ: .loading-* など）
```

**理由**:
- コンポーネントとスタイルの依存関係が明確
- 不要なコンポーネント削除時にスタイルも一緒に削除できる
- CSS モジュールはクラス名の競合を防ぐ
- 業界標準の実践パターン

---

### 1.3 CSS のクラス命名規則の問題

#### 問題: 命名に一貫性がない
- **現状**: `.loading-container`, `.main-title`, `.logo-img`, `.dots-container`, `.loading-text`, `.dot`
- **問題点**:
  - 一部は日本語的な階層構造（`.loading-container > .dots-container`）
  - 一部は単語の羅列（`.main-title`, `.logo-img`）
  - コンポーネント名との対応が不明確
  - 他のクラスとの競合の可能性（`.main-title` は Loading 内でのみ使用か不明確）

#### 現在のクラス構造の問題:
```
.app-shell              ← App 全体のラッパー
  .title-container      ← タイトルエリア
    .main-title         ← h1 タイトル
    .sub-title          ← h2 サブタイトル
  .logo-container       ← ロゴエリア
    .logo-img           ← img タグ
  .loading-container    ← Loading 機能
    .loading-text       ← テキスト
    .dots-container     ← ドットコンテナ
      .dot              ← ドット（複数）
```

#### 推奨命名パターン（BEM 風になってくるが、明確性のため）:
```
方式1: コンポーネント接頭辞付き（推奨 - スコープ明確）
.loading                ← コンポーネント ルート
.loading__header        ← ヘッダーセクション
.loading__title         ← タイトル
.loading__subtitle      ← サブタイトル
.loading__logo          ← ロゴ
.loading__content       ← メインコンテンツエリア
.loading__indicator     ← ローディング インジケーター
.loading__text          ← テキスト
.loading__dots          ← ドットコンテナ
.loading__dot           ← ドット（単数）

方式2: OOCSS 的なシンプル名（小規模向け）
.loading                ← ルート
.title                  ← タイトル
.subtitle               ← サブタイトル
.logo                   ← ロゴ
.spinner                ← スピナー
.spinner__text          ← スピナーテキスト
.spinner__dot           ← スピナードット
```

#### 更に詳細な問題:
1. `.app-shell` という名称：
   - 「app-shell」は PWA や SPA アーキテクチャ用語
   - 単一ページのレイアウト全体を指すには不適切
   - より適切な名前：`.page-root`, `.page-container`, `.page`, `.loading-page`

2. `.loading-container` が二度定義されている：
   - CSS の10行目と49行目に同じセレクタが定義
   - 49行目の定義が10行目を上書き
   - この矛盾は管理が困難

3. `user-select: none` の重複：
   - ほぼすべてのクラスに含まれている
   - グローバル CSS で一度定義すべき
   - ベンダープレフィックスの完全性も疑問

---

### 1.4 JSX の構成と命名の問題

#### 問題: ファイル名とエクスポート関数名
- **現状**: `Loading.jsx` with `export default function Loading()`
- **問題点**:
  - 単一ページアプリケーションを表示するメインコンポーネントが「Loading」という機能名
  - ページの目的や内容が不明確
  - 実際には「Loading ページ」や「テンプレート表示ページ」であり、「ローディング中の表示」ではない混乱

#### 推奨ファイル名:
- `Home.jsx` （ホームページの場合）
- `Index.jsx` （初期ページの場合）
- `Splash.jsx` （スプラッシュスクリーン的な場合）
- `Template.jsx` （テンプレート表示専用の場合）

#### 推奨ファイル構造:
```jsx
// Loading.jsx -> Home.jsx もしくは Index.jsx に改名
import Loading from '../components/loading/Loading';

export default function Home() {
  return (
    <div className="home-page">
      {/* ページレイアウト */}
      <Loading />
    </div>
  );
}
```

---

### 1.5 JSX の内部構造の問題

#### 問題: 関心の分離が不十分
- **現状**: 1つの大きなコンポーネント内に複数の関心が混在
  - タイトル表示（`.title-container`）
  - ロゴ表示（`.logo-container`）
  - ローディング表示（`.loading-container`）

- **問題点**:
  - ページレイアウト、ヘッダー、ロゴ、ローディング機能が分離できない
  - Loading コンポーネントの再利用ができない
  - ローディング動作だけをテストできない

#### 推奨構成: 関心の分離
```
src/
  components/
    loading/                  ← Loading 機能専用
      Loading.jsx            （ローディング表示のみ）
      Loading.module.css
    logo/                     ← ロゴ表示専用
      Logo.jsx
      Logo.module.css
    header/                   ← ヘッダー専用
      Header.jsx
      Header.module.css
  pages/
    Home.jsx  もしくは Template.jsx
```

```jsx
// pages/Home.jsx (改善案)
import Header from '../components/header/Header';
import Loading from '../components/loading/Loading';
import './home.css';

export default function Home() {
  return (
    <div className="home-page">
      <Header />
      <main className="home-main">
        <Loading />
      </main>
    </div>
  );
}
```

```jsx
// components/loading/Loading.jsx (改善案)
import './Loading.module.css';

export default function Loading() {
  return (
    <div className={styles.loading}>
      <p className={styles.text}>Loading</p>
      <div className={styles.dots}>
        <span className={styles.dot}></span>
        <span className={styles.dot}></span>
        <span className={styles.dot}></span>
      </div>
    </div>
  );
}
```

---

### 1.6 CSS の重複定義

#### 問題: `.loading-container` の定義が2箇所
- **1回目**: 10行目 - `flex, justify-content, align-items, padding, user-select`
- **2回目**: 49行目 - `flex-direction: column, justify-content, align-items, user-select`

**結果**: 1回目の定義が上書きされ、2回目のプロパティ（`flex-direction: column`）のみ有効

#### 推奨: 統一の定義
```css
.loading-container {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 10px;
  user-select: none;
}
```

---

### 1.7 ベンダープレフィックスの問題

#### 問題: 古い記述方法
- **現状**: すべての `user-select` に対して以下を記述
  ```css
  user-select: none;
  -webkit-user-select: none;
  -moz-user-select: none;
  -ms-user-select: none;
  ```

- **問題点**:
  - Modern Browsers（Chrome 44+, Firefox 69+, Safari 12.1+, Edge 79+）では `-webkit-` プレフィックスのみで十分
  - `-moz-` や `-ms-` は既に不要（2024-2025年時点）
  - コードの冗長性が高い
  - メンテナンスが困難

#### 推奨:
```css
/* グローバルで一度定義 */
body, body * {
  -webkit-user-select: none;
  user-select: none;
}

/* または必要な場所だけ */
.loading {
  -webkit-user-select: none;
  user-select: none;
}
```

---

### 1.8 CSS の セマンティック性

#### 問題: クラス名と実装の乖離
- `.logo-container` + `.logo-img`:
  - 画像を単に「ロゴ」とは限らない（実装では tiger.svg）
  - フォーカス可能か？ alt テキストは？ 意図不明

- `.main-title` + `.sub-title`:
  - 「メイン」「サブ」は相対的 → デバイスサイズによって変わる可能性
  - より説明的な命名：`.page-title`, `.page-subtitle` など

---

### 1.9 JavaScript の問題

#### 問題: 環境判定ロジック
```jsx
const subTitle = import.meta.env.DEV ? "Development" : "Production";
```

- **問題点**:
  - UI に環境情報を表示することは通常ベストプラクティスではない
  - テンプレート表示の場合、この情報は不要かもしれない
  - ページの目的が不明確（テンプレート表示か、実際のアプリケーション初期化か？）

#### 推奨:
- テンプレート表示ならば削除
- 実際のアプリケーションなら `.env` で管理し、必要に応じてデバッグモード表示に限定

---

### 1.10 アニメーション定義の欠落

#### 問題: `animation: bounce` が定義されていない
- CSS の79行目で `.dot` に `animation: bounce 1.4s ease-in-out infinite both;` を指定
- しかし `@keyframes bounce` の定義が無い

**結果**: アニメーションが動作しない（エラー出力の可能性）

#### 推奨: @keyframes の定義を追加
```css
@keyframes bounce {
  0%, 80%, 100% {
    transform: scale(0);
    opacity: 0.5;
  }
  40% {
    transform: scale(1);
    opacity: 1;
  }
}
```

---

## 2. 改善案の優先度

### 高優先度（必須修正）
1. **ファイル構成**: `pages/Loading.jsx` → `components/loading/Loading.jsx` + `pages/Home.jsx` (or similar)
2. **CSS ファイル配置**: グローバル `styles/loading.css` → コンポーネント内 `Loading.module.css`
3. **アニメーション定義**: `@keyframes bounce` を追加
4. **重複 CSS の削除**: `.loading-container` の二重定義を統一
5. **ファイル命名**: `Loading.jsx` → `Home.jsx` 等へ改名（ページ内容に応じて）

### 中優先度（推奨修正）
1. **クラス命名の統一**: コンポーネント接頭辞の統一
2. **CSS の冗長性削減**: `user-select` をグローバルで定義
3. **ベンダープレフィックス削減**: 古い記述方法から最小限に
4. **コンポーネント分離**: Header, Logo, Loading を独立したコンポーネントへ

### 低優先度（将来改善）
1. **関心の分離**: より細かなコンポーネント設計
2. **スタイリング方式**: CSS Modules or styled-components の選択
3. **環境情報の表示**: 実装パターンの確定

---

## 3. 参考: 日本で使われる一般的な React 構成

### 典型的なプロジェクト構成
```
src/
├── assets/           # 画像、フォント等
├── components/       # 再利用可能なコンポーネント
│   ├── common/      # 全ページで使用する共通コンポーネント
│   │   ├── Header.jsx
│   │   ├── Footer.jsx
│   │   └── Navigation.jsx
│   ├── ui/          # UI パターン（Button, Card など）
│   ├── loading/     # Loading コンポーネント
│   └── ...
├── pages/           # ページコンポーネント（ルーティング対象）
│   ├── Home.jsx
│   ├── About.jsx
│   └── ...
├── hooks/           # カスタムフック
├── services/        # API 通信など
├── utils/           # ユーティリティ関数
├── styles/          # グローバルスタイル
│   └── global.css
├── App.jsx
└── main.jsx
```

### クラス命名の一般的なパターン（日本企業）
- **シンプル方式**: `.[コンポーネント名]-[役割]` 例: `loading-container`, `button-primary`
- **階層方式**: `.[親]-[子]-[役割]` 例: `header-nav-item`, `modal-content-title`
- **OOCSS 方式**: `.u-[機能]` (utility), `.c-[component]` (component)

---

## まとめ

このプロジェクトは以下の改善が必要です：

| 項目 | 現状 | 推奨 | 重要度 |
|-----|------|------|--------|
| ファイル構成 | pages/Loading.jsx | components/loading/Loading.jsx + pages/Home.jsx | 🔴 高 |
| CSS 配置 | styles/loading.css | components/loading/Loading.module.css | 🔴 高 |
| クラス命名 | 混在 | 統一的なパターン | 🟡 中 |
| アニメーション | 未定義 (@keyframes 無し) | @keyframes bounce 追加 | 🔴 高 |
| 重複定義 | .loading-container 2回 | 1度の定義に統一 | 🔴 高 |
| ベンダープレフィックス | 4行記述 | 1-2行に削減 | 🟡 中 |
| 関心の分離 | 1 ファイル | 複数コンポーネント分割 | 🟡 中 |

**初心者エンジニアの見本として提示する場合**、高優先度項目の修正は**必須**です。これにより、業界標準的な構造を示すことができます。
